Változók:
- Int
- Float, Double
- String, Char
- Boolean

- Array, ArrayList
- Int[] tomb = new Int[50];
- Int[][] matrix = new Int[3][3];

Kompononsek:
- Button
- TextView
- EditView
- Layout-ok (Relative, Linear, Table, Absolute)

- Floating Action Bar
- TextInputEditText
- TextInputLayout
- Toolbar

- Notificaton (Manager, Builder)
- Services

- Timer

Funkciók:
- findViewById(R.id.valami);
- R (Resource) -> Komponens, változó tároló
- onClick(), setOnClickListener(new OnClickListener), implements View.OnClickListener-->public void onClick(View v);
- getText().toString(); <-- Text
- setText();
- setError(); <--- Error message állítás
- requestFocus(); komponensre való fókuszálás 

- .equals() / .equalsIgnoreCasing();
- .isEmpty();

- startIntent(intent);
- intent.putExtra(USER_ARRAY, User); <---- Milyen néven, mit adunk át annak az intentnek amit átadunk

- startService(service);
- stopService(service);

- getMenuInflater().inflate(R.menu.main, menu); <--- Menu.xml adott activiy-hez való fűzése
- getitemId(); <--- ID int visszaadása

- scheduleAtFixedRate((new TimerTask{}) {}, StartDelay, Interval);

- onDestroy(); <--- Intent/service/komponens törlése, likvidálása
- onCreate(); <--- Interface generálás, Komponensek és változók deklarálása
